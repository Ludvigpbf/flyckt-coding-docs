
type AboutProps = {className?: string};

const ComponentName: React.FC<AboutProps> = (className) => {
    return (
        <div className={`${className}`}>
            
        </div>
    );
}

export default ComponentName;